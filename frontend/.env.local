# Backend API URL (internal network)
# Since both services run on the same machine, Next.js can call localhost
# This works because Next.js makes server-side API calls
BACKEND_API_URL=http://localhost:8000
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
