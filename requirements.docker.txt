# Ouroboros SDK — Docker requirements (slim)
# Excludes heavy/optional packages that cause build issues in containers:
#   - pyrit (complex native deps)
#   - torch/transformers (not needed when using Ollama)
#   - playwright (browser automation, not needed in container)
#   - weasyprint (complex native deps)

# ── Core framework ──
langchain==1.2.7
langgraph==1.0.7
ollama==0.6.1
langchain-community==0.4.1
langchain-core==1.2.7

# ── Scanning ──
semgrep==1.149.0
checkov>=3.2.0

# ── LLM (Ollama-only, no local models) ──
anthropic==0.76.0
openai==2.15.0

# ── Web framework ──
fastapi==0.128.0
uvicorn==0.40.0
pydantic==2.12.5
pydantic-settings==2.12.0
email-validator>=2.0.0
python-multipart==0.0.21

# ── Integrations ──
requests==2.32.5
PyGithub==2.8.1
google-api-python-client==2.188.0
google-auth==2.47.0
mcp==1.23.3

# ── Database & storage ──
psycopg2-binary==2.9.11
redis==7.1.0
immudb-py==1.5.0
SQLAlchemy==2.0.46

# ── Security ──
cryptography==46.0.3
python-jose==3.5.0
passlib==1.7.4
argon2-cffi==25.1.0
python-dotenv==1.2.1

# ── Monitoring & utilities ──
prometheus-client==0.24.1
docker==7.1.0
pyyaml==6.0.3
jinja2==3.1.6
tenacity==9.1.2
rich==13.5.3
click==8.1.8

# ── PDF generation ──
markdown==3.7.0
reportlab==4.1.0

# ── HTTP ──
httpx==0.28.1
