feat: ouroboros-sdk v1.0.0 production build

SDK packaging of the Ouroboros AI security platform as a pip-installable package.

New files:
  - pyproject.toml — Poetry build config with all dependencies
  - ouroboros/__init__.py — Package entry-point (v1.0.0)
  - ouroboros/core.py — Central orchestrator wrapping LangGraph workflow
  - ouroboros/cli.py — Click CLI: scan, watch, info commands
  - ouroboros/github.py — GitHub integration (clone, PR creation)
  - ouroboros/fixes.py — Fix generator (BLUE Agent + Jinja2 fallback)
  - ouroboros/docs.py — PDF security report (ReportLab)
  - ouroboros/continuous.py — Watch mode (periodic re-scan)
  - config.example.yaml — Example configuration template
  - templates/*.j2 — Jinja2 patch templates (API hardening, Docker)
  - examples/demo.py — End-to-end demo script
  - tests/test_sdk.py — 15 smoke tests (all passing)
  - .github/workflows/publish.yml — PyPI publish on tagged release

Integration:
  - core.py wraps OuroborosWorkflow.run() for full pipeline
  - Standalone helpers: detect_vulnerabilities(), generate_fixes(), create_pr()
  - CLI entry-point: ouroboros scan/watch/info
  - 15/15 tests passing, wheel builds cleanly

Build artifacts:
  - dist/ouroboros_sdk-1.0.0-py3-none-any.whl
  - dist/ouroboros_sdk-1.0.0.tar.gz
